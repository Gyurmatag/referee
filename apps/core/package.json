{
  "name": "@referee/core",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 execute referee-db --local --file=src/db/schema.sql"
  },
  "dependencies": {
    "@cloudflare/sandbox": "0.12.9",
    "@referee/shared": "workspace:*",
    "hono": "^4.9.8",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "@types/node": "^22.20.4",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.40.0"
  }
}
